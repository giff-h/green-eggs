from typing import List, Pattern

USERNAME_MAX_LENGTH: int
MESSAGE_MAX_LENGTH: int
USERNAME_REGEX: Pattern[str]
EXPECTED_AUTH_CODES: List[str]
AUTH_EXPECT_PATTERN: Pattern[str]
CAP_REQ_MODES: List[str]
CAP_ACK_PATTERN: Pattern[str]
EXPECTED_JOIN_CODES: List[str]
JOIN_EXPECT_PATTERN: Pattern[str]
PART_EXPECT_PATTERN: Pattern[str]
RECONNECT_PATTERN: Pattern[str]
PRIVMSG_PATTERN: Pattern[str]
JOIN_PART_PATTERN: Pattern[str]
CLEARCHAT_PATTERN: Pattern[str]
USERNOTICE_PATTERN: Pattern[str]
ROOMSTATE_PATTERN: Pattern[str]
USERSTATE_PATTERN: Pattern[str]
CLEARMSG_PATTERN: Pattern[str]
NOTICE_PATTERN: Pattern[str]
HOSTTARGET_PATTERN: Pattern[str]
CODE_353_PATTERN: Pattern[str]
CODE_366_PATTERN: Pattern[str]
WHISPER_PATTERN: Pattern[str]
URL_PATTERN: Pattern[str]
